#!/bin/bash
MODE=$1
HOSTS_IPS=$2

echo -e ${HOSTS_IPS//'n'/'\n'} > hosts

if [[ $MODE == 'create-cluster' ]]; then
  ansible-playbook -i /ansible/hosts /ansible/create-swarm-cluster.yml
fi

if [[ $MODE == 'deploy' ]]; then
  ansible-playbook -i /ansible/hosts /ansible/deploy-stack.yml
fi